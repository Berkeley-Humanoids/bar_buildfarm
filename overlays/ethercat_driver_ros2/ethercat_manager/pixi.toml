# OVERLAY (copied into src/ethercat_driver_ros2/ethercat_manager/ by apply-overlays.sh).
# BLOCKED: links IgH EtherLab libethercat (no conda pkg). See packages-blocked.txt.
[package.build.backend]
name = "pixi-build-ros"
version = "*"
channels = ["https://prefix.dev/conda-forge"]

[package.build.config]
distro = "jazzy"
