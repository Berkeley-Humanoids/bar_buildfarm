# OVERLAY (copied into src/ethercat_driver_ros2/ethercat_driver_ros2/ by apply-overlays.sh).
# Metapackage tying the ethercat_* set together.
[package.build.backend]
name = "pixi-build-ros"
version = "*"
channels = ["https://prefix.dev/conda-forge"]

[package.build.config]
distro = "jazzy"
