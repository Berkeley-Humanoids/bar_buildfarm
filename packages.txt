# Packages the buildfarm builds, in dependency order. One src-relative path per
# line; blank lines and #-comments ignored. Grown as overlays land (see
# packages-blocked.txt for the not-yet-buildable set).
#
# --- first-party bar_* (build with RoboStack + conda-forge only) ---
src/bar_ros2/bar_msgs
src/bar_ros2/bar_common
src/bar_ros2/bar_description_lite
src/bar_ros2/bar_description_prime
src/bar_ros2/bar_drivers/bar_socketcan
src/bar_ros2/bar_devices/bar_robstride
src/bar_ros2/bar_devices/bar_sito
src/bar_ros2/bar_controllers
src/bar_ros2/bar_policy
src/bar_ros2/bar_cli
src/bar_ros2/bar_bringup_lite
#
# --- third-party (sim path) ---
# mujoco_ament FetchContents the MuJoCo binary at configure time → the build
# needs network (rattler-build allows it; non-hermetic). If this proves flaky,
# repackage the MuJoCo binary as its own conda dep. The other two are plain ament.
src/mujoco_ament
src/mujoco_sim_ros2
src/mujoco_ros2_control/mujoco_ros2_control
